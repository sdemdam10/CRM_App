<Window x:Class="CRM_App.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Please enter your login credentials"/>

        <Label Grid.Row="1" Content="Username"/>
        <TextBox Grid.Row="1" x:Name="UsernameTextBox"/>

        <Label Grid.Row="2" Content="Password"/>
        <PasswordBox Grid.Row="2" x:Name="PasswordBox"/>

        <Button Grid.Row="3" Content="Login" Click="LoginButton_Click"/>

        <TextBlock Grid.Row="4" x:Name="ErrorMessageTextBlock" Foreground="Red" Visibility="Collapsed"/>

    </Grid>
</Window>
